{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "prepare": "husky install",
    "lint": "eslint --ext .js,.cjs,.ts ."
  },
  "devDependencies": {
    "@antfu/eslint-config": "^0.36.0",
    "eslint": "^8.36.0",
    "husky": "^8.0.3",
    "lint-staged": "^13.2.0",
    "nuxt": "^3.3.1"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint"
    },
    "lint-staged": {
      "*.{js,vue,jsx,ts,tsx}": "npm run lint:fix"
    }
  }
}
